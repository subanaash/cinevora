[project]
name = "cinevora"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "SubanaSharma", email = "subonasharma314@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "google-adk>=2.6.3",
    "parallel-web>=1.1.0",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.52.1",
]

[project.scripts]
cinevora = "cinevora:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
